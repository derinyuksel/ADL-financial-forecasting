# ADL Financial Forecasting — Python dependencies
#
# Pin to versions known to be compatible with the notebooks and baseline_lstm.py.
# Tested on Google Colab (Python 3.11).
#
# Install with:
#   pip install -r requirements.txt

# Core scientific stack
numpy>=1.26,<2.2
pandas>=2.1,<2.3
scipy>=1.11

# Deep learning
torch>=2.2
# Torchvision/torchaudio are not required for this project.

# Classical ML / utilities
scikit-learn>=1.3

# Data acquisition
yfinance>=0.2.40        # Yahoo Finance (BIST 100, S&P 500)
fredapi>=0.5            # FRED-MD macro series
pandas-datareader>=0.10 # fallback for macro fetches

# Visualisation
matplotlib>=3.7
seaborn>=0.13

# Jupyter (used for /notebooks)
jupyter>=1.0
ipykernel>=6.25
